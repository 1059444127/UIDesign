<Window x:Class="Monitor.windows.exam_Archive"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Local="clr-namespace:Monitor.Exam"
        Title="Archive" Height="573" Width="592"
        Style="{StaticResource WindowStyle}"
        x:Name="Window"
	    WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <!--<Window.Tag>
        <StackPanel Orientation="Horizontal">
            <Button Content="H" Margin="0,0,2,0" Command="{Binding}" BorderThickness="0"/>
            <Button Content="B" Margin="0,0,2,0" Command="{Binding}" BorderThickness="0"/>
            <Button Content="T" Margin="0,0,2,0" Command="{Binding}" BorderThickness="0"/>
            <Button Content="T" Margin="0,0,2,0" Command="{Binding}" BorderThickness="0"/>
            <Button Content="T" Margin="0,0,2,0" Command="{Binding}" BorderThickness="0"/>
            <Button Content="T" Margin="0,0,2,0" Command="{Binding}" BorderThickness="0"/>
        </StackPanel>
    </Window.Tag>-->
    <Grid>
        <Local:ArchivePanel/>
    </Grid>
</Window>
